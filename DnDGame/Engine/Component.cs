

namespace DnDGame.Engine
{
	/// <summary>
	/// The simple interface for a component. Components are very generic objects, which will vary greatly in their features,
	/// however they all inherit from IComponent so that they may be grouped together.
	/// </summary>
    public interface IComponent
    {
    }
}
